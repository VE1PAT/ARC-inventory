# Deny direct web access to non-public project folders.
# Used when the whole repo sits under the site web root (e.g. public_html).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
